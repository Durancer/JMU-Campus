spring:
  application:
    name: gateway-server
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        shared-configs:
          - data-id: public-security.yaml
            refresh: true
          - data-id: default-redis.yaml
            refresh: true

